const personagem = document.querySelectorAll('#input-personagem');
const atributos = document.querySelectorAll('#atributo');

const For = parseInt(atributos[0].innerHTML)
const Des = parseInt(atributos[1].innerHTML)
const Con = parseInt(atributos[2].innerHTML)
const Int = parseInt(atributos[3].innerHTML)
const Sab = parseInt(atributos[4].innerHTML)
const Car = parseInt(atributos[5].innerHTML)


